# Development environment overrides for nginx-ingress

controller:
  # Single replica for dev
  replicaCount: 1

  # Lower resources for dev
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 256Mi
